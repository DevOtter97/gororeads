---
import BaseLayout from './BaseLayout.astro';

interface Props {
  title: string;
}

const { title } = Astro.props;
---

<BaseLayout title={title}>
  <div class="auth-page">
    <div class="auth-card">
      <slot />
    </div>
  </div>
</BaseLayout>
